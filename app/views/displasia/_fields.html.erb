<h4>2. Displasia cortical focal (DCF)</h4>
<small>Zonas a ser inspeccionadas: Frontal-parietal>temporal>occipital</small>
<h6>Hallazgos a explorar:</h6>

<%= rf.simple_fields_for :displasium, @report.displasium do |f| %>

  <%= f.input :hiperintensidad, collection: @laterality, as: :radio_buttons, item_wrapper_class: 'inline',
                    label: 'Hiperintensidad sustancia blanca en FLAIR y T2:' %> <br>

  <%= f.input :diferencicacion_grisblanca, collection: @laterality, as: :radio_buttons, item_wrapper_class: 'inline',
                    label: 'Diferenciación de sustancia gris y blanca. Anormal?' %>

  <%= f.input :engrosamiento, collection: @laterality, as: :radio_buttons, item_wrapper_class: 'inline',
                    label: 'Engrosamiento focal cortical:' %>

  <%= f.input :transmato, collection: @laterality, as: :radio_buttons, item_wrapper_class: 'inline',
                    label: 'Signo transmanto en FLAIR:' %>

  <%= f.input :circunvoluciones, collection: @laterality, as: :radio_buttons, item_wrapper_class: 'inline',
                    label: 'Patrón de circunvoluciones y surcos. Anormal?:' %>

 <!-- <%= f.input :reformateo, collection: @laterality, as: :radio_buttons, item_wrapper_class: 'inline',
                    label: 'Reformateo curvilíneo: patrón de surcos. Anormal?' %>-->

  <h6>Conclusión: Displasia cortical en</h6>
  <%= f.input :discircunvolucion, label: 'Circunvolución:', :input_html => { :rows => 1} %>
 
<%= f.input :dislobulo, collection: @lobe, as: :select, default: 1,label: 'Lóbulo' %>
<!--<%= f.input :dishemisferio, collection: @hemisphere, as: :select, default: 1 ,label: 'Hemisferio' %>
<em><%= f.input :dislobulo2, collection: @lobe, as: :select, default: 1,label: 'Lóbulo 2 '%>
	<%= f.input :dishemisferio2, collection: @hemisphere, as: :select, default: 1,label: ' Hemisferio 2'%>
<%= f.input :dislobulo3, collection: @lobe, as: :select, default: 1,label: 'Lóbulo 3' %>
<%= f.input :dishemisferio3, collection: @hemisphere, as: :select, default: 1,label: 'Hemisferio 3'%>
<%= f.input :dislobulo4, collection: @lobe, as: :select, default: 1,label: 'Lóbulo 4'%>
<%= f.input :dishemisferio4, collection: @hemisphere, as: :select, default: 1,label: ' Hemisferio 4'%></em>
-->
<!-- lo anterior se puso en comentario para imprimir los reportes-->

 <em> </em>

<% end %>